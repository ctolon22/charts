kind: .
metadata:
  name: .
spec:
  template:
    spec:
      containers:
      - name: pause
        securityContext:
          runAsNonRoot: true
      initContainers:
      - name: nvidia-driver-installer
        securityContext:
          runAsNonRoot: true
